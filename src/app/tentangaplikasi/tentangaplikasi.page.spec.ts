import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TentangaplikasiPage } from './tentangaplikasi.page';

describe('TentangaplikasiPage', () => {
  let component: TentangaplikasiPage;
  let fixture: ComponentFixture<TentangaplikasiPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TentangaplikasiPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
